orion.dictionary.addDefinition('homeDividerImage', 'parallax',
  orion.attribute('image', {
      label: 'Home Page Divider',
      optional: true
  })
);

orion.dictionary.addDefinition('topNav', 'parallax',
  orion.attribute('image', {
      label: 'Top Nav',
      optional: true
  })
);

orion.dictionary.addDefinition('testimonials', 'parallax',
  orion.attribute('image', {
      label: 'Testimonials Section',
      optional: true
  })
);
