orion.dictionary.addDefinition('title', 'team members(page)', {
	type: String,
	label: 'Title',
	optional: true
});

orion.dictionary.addDefinition('content', 'team members(page)', {
	type: String,
	label: 'Content',
	optional: true
});